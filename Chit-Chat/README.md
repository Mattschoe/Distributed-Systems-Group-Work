# HOW TO RUN
1. Navigate to ``./server`` and run ``go run server.go`` in the terminal
2. In another terminal, navigate to ``./client`` and run ``go run client.go``
3. Repeat step 2. as many times for as many clients wished for
4. For leaving as client or shutting down the server, use command ``.quit`` in either terminal